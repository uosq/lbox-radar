local config = {
    size = 200,
    icon_size = 12,
    zoom = 0.1, -- 10% zoom
    circular = false,
    healthbar = true,
    background_transparency = 250,
    light_mode = false,
    world_locked = false,
    icon_outline = true,
}

return config
